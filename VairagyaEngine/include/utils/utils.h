#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <vector>

using namespace std;

bool isValidPath(const string& path);
    
vector<string> fetchLinesFromFile(const string& path);

#endif // UTILS_H
